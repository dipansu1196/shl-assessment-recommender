# Core dependencies for SHL Assessment Recommender
# Optimized for Render 512MB tier - CPU only via pip.conf

# Embeddings (lightweight model) - will pull torch CPU-only via pip.conf index
sentence-transformers>=2.2.0
transformers>=4.30.0
faiss-cpu>=1.7.4

# API framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
uvloop>=0.19.0
httptools>=0.6.0
pydantic>=2.0.0

# LLM client
groq>=0.4.0

# Utilities
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
