# Core dependencies for SHL Assessment Recommender

# PyTorch CPU-only via extra index (avoids CUDA wheels)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu

# Embeddings - pinned for compatibility with torch 2.1 + huggingface_hub
sentence-transformers==3.0.1
transformers==4.44.2
huggingface_hub==0.24.7
faiss-cpu>=1.7.4

# API framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
uvloop>=0.19.0
httptools>=0.6.0
pydantic>=2.0.0

# LLM client
groq>=0.4.0

# Utilities
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
