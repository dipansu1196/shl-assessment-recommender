# Core dependencies for SHL Assessment Recommender
# Optimized for Render 512MB tier - CPU only, NO CUDA

# PyTorch CPU-only (must be installed first via custom PyTorch index in render.yaml)
torch>=1.11.0

# Embeddings (lightweight model) - CPU only
sentence-transformers>=2.2.0
transformers>=4.30.0
faiss-cpu>=1.7.4

# API framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
uvloop>=0.19.0
httptools>=0.6.0
pydantic>=2.0.0

# LLM client
groq>=0.4.0

# Utilities
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
